/**
 * 求和
 * 
 * @param {*} a 
 * @param {*} b 
 * @returns 
 */
function sum(a, b) {
  return a + b
}

/**
 * 返回原值
 * 
 * @param {*} a 
 * @returns 
 */
function val(a) {
  return a
}

module.exports = {
  sum,
  val
}